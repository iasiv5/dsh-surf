# dsh-surf

让人类（而非 agent）通过远程服务器的网络出口沉浸式上网的浏览器系统：一个跑在服务器上的真实 Chrome，以视频流形态推到用户的本地浏览器里。本文件是这一语境的术语表。

## Language

### 浏览器与身份

**Surf 浏览器**:
跑在远程服务器上、供人类直接驾驶的 Chrome——用户看到的"这台浏览器"。网络出口为服务器所在地的 IP。
_Avoid_: 云浏览器、VPN、Agent 浏览器

**Agent 浏览器**:
ego-browser 插件管理的、由 agent 通过 `ego_*` 工具驾驶的浏览器。与 Surf 浏览器是完全独立的两套浏览器栈，互不共享实例、profile 与登录态。
_Avoid_: 把两者混称"服务器浏览器"

**登录态**:
Surf 浏览器内持久存在的已登录身份（Google 等）。重启、升级、重建容器后依然保留；它整体代表"这台设备交出了哪些账号"。
_Avoid_: profile、cookie 目录

**出口**:
网站看到的网络身份：部署服务器的 IP 与所在地。是本系统存在的根本理由。
_Avoid_: VPN 节点、代理出口

### 传输与使用

**推流会话**:
用户本地浏览器与 Surf 浏览器之间的一条"画面 + 声音 + 输入"实时连接。一次冲浪 = 一条推流会话；v1 同时只存在一条。
_Avoid_: 观察窗（那是 ego-browser 的术语）、watch、直播

**流量账**:
计费事实：只有推流会话与文件取出计入服务器出流量；Surf 浏览器自己访问网站的内容是入站、不计费。
_Avoid_: 流量包（那是云厂商侧的词）

### 部署与边界

**边缘链**:
所有公网流量的必经之路：入口 → dsh-auth 认证 → 环回服务。Surf 浏览器不做自己的认证，它的门就是边缘链的门。
_Avoid_: 网关（易与 dsh-auth-caddy 混淆）

**入口**:
边缘链对公网开放的两扇前门之一（A：IP 直连 Caddy；B：域名经 nginx）。同一边缘链，两条到达路径，互为备份。
_Avoid_: 域名/端口混称

**薄插件**:
dsh-surf 在 DSH 里的全部存在形式：入口按钮与新标签页打开——只指路，不承载浏览。浏览栈与 DSH 插件生命周期解耦。
_Avoid_: 浏览器插件（它不浏览任何东西）

**Surf 镜像**:
在官方 linuxserver/chrome 之上叠加中文输入法与字体的自定义容器镜像，是带版本历史的制品，升级 = 重建。
_Avoid_: docker 镜像（语境内特指我们维护的这一枚）
